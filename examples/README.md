Run the sidecar example with the following commands:

```sh
# Clone the git repo to local
git clone git@github.com:huan/sidecar.git

# Enter the repo folder
cd sidecar

# Install dependencies
npm install

# run the examples/demo.ts
npm start
```
