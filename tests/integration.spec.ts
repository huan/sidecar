#!/usr/bin/env ts-node

/* eslint-disable padded-blocks */

import { test }  from 'tstest'

import path from 'path'

import {
  Sidecar,
  API,
  RETURN,
}                   from '../src/mod'

test('test', async (t) => {
  void path
  void Sidecar
  void API
  void RETURN
  t.skip('tbw')
})
